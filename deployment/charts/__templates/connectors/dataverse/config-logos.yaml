{{ if and (hasKey .Values.connectors.datasafe "logos") (or (and (hasKey .Values.connectors.datasafe.logos "default") (.Files.Get .Values.connectors.datasafe.logos.default)) (and (hasKey .Values.connectors.datasafe.logos "horizontal") (.Files.Get .Values.connectors.datasafe.logos.horizontal))) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.connectors.datasafe.componentName }}-logos-config"
  namespace: {{ .Values.namespace }}

binaryData:
{{ if and (hasKey .Values.connectors.datasafe "logos") (hasKey .Values.connectors.datasafe.logos "default") }}
  logo.png: |-
    {{ .Files.Get .Values.connectors.datasafe.logos.default  | b64enc  }}
{{ end }}
{{ if and (hasKey .Values.connectors.datasafe "logos") (hasKey .Values.connectors.datasafe.logos "horizontal") }}
  horizontal.png: |-
    {{ .Files.Get .Values.connectors.datasafe.logos.horizontal  | b64enc  }}
{{ end }}
{{ end }}
